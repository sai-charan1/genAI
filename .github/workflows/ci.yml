name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install test deps
        run: |
          pip install --upgrade pip
          pip install pytest ruff black numpy rank-bm25 python-dotenv
      - name: Ruff
        run: ruff check ingestion/ eval/ tests/ --ignore E501
      - name: Pytest
        run: pytest tests/ -v
